name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Lint YAML files
        run: |
          pip install yamllint
          yamllint gitops/ charts/ || true
      
      - name: Lint Terraform
        run: |
          cd infra/terraform/bootstrap
          terraform fmt -check -recursive || true
      
      # TODO Phase 1+: Add Python linting (black, isort, pylint)
      # TODO Phase 1+: Add Go linting (golangci-lint)

  terraform-validate:
    name: Validate Terraform
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0
      
      - name: Terraform Init
        run: |
          cd infra/terraform/bootstrap
          terraform init -backend=false
      
      - name: Terraform Validate
        run: |
          cd infra/terraform/bootstrap
          terraform validate

  # TODO Phase 1+: Add unit tests job
  # unit-tests:
  #   name: Unit Tests
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Run Python tests
  #       run: pytest apps/copilot-api/tests/
  
  # TODO Phase 2+: Add integration tests
  # integration-tests:
  #   name: Integration Tests
  #   runs-on: ubuntu-latest
  #   needs: [lint, terraform-validate]
  #   steps:
  #     - name: Create kind cluster
  #       run: kind create cluster
  #     - name: Run integration tests
  #       run: pytest tests/integration/
  
  # TODO Phase 5+: Build and push Docker images
  # build:
  #   name: Build Images
  #   runs-on: ubuntu-latest
  #   needs: [lint, unit-tests]
  #   steps:
  #     - name: Build simulator image
  #       run: docker build -t iot-simulator:${{ github.sha }} apps/simulator/
